<h3>Home</h3>

	<ul id ="posts">
		<% @posts.each do |post| %>
	    <p>
			<%= link_to post.title, post_path(post) %> |
			<%= link_to 'Edit post', edit_post_path(post) %>
		</p>
		<% end %>
	</ul>
	<%= link_to 'New', new_post_path %>
